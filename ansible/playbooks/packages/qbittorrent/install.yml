---
- hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: qbittorrent install placeholder
      ansible.builtin.debug:
        msg: "brrewery stub playbook for qbittorrent install"
