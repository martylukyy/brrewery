---
- hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: rutorrent remove placeholder
      ansible.builtin.debug:
        msg: "brrewery stub playbook for rutorrent remove"
