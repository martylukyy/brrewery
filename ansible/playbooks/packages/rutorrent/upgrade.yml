---
- hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: rutorrent upgrade placeholder
      ansible.builtin.debug:
        msg: "brrewery stub playbook for rutorrent upgrade"
