---
- hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: sabnzbd install placeholder
      ansible.builtin.debug:
        msg: "brrewery stub playbook for sabnzbd install"
