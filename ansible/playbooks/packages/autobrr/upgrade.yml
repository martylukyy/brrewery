---
- hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: autobrr upgrade placeholder
      ansible.builtin.debug:
        msg: "brrewery stub playbook for autobrr upgrade"
