---
- hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: jellyfin install placeholder
      ansible.builtin.debug:
        msg: "brrewery stub playbook for jellyfin install"
