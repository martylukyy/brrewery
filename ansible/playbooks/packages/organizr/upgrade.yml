---
- hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: organizr upgrade placeholder
      ansible.builtin.debug:
        msg: "brrewery stub playbook for organizr upgrade"
