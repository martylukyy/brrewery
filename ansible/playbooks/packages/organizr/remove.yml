---
- hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: organizr remove placeholder
      ansible.builtin.debug:
        msg: "brrewery stub playbook for organizr remove"
