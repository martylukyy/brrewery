---
- hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: prowlarr upgrade placeholder
      ansible.builtin.debug:
        msg: "brrewery stub playbook for prowlarr upgrade"
