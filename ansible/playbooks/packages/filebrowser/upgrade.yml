---
- hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: filebrowser upgrade placeholder
      ansible.builtin.debug:
        msg: "brrewery stub playbook for filebrowser upgrade"
