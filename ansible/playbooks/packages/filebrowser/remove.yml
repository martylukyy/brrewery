---
- hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: filebrowser remove placeholder
      ansible.builtin.debug:
        msg: "brrewery stub playbook for filebrowser remove"
