---
- hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: rtorrent remove placeholder
      ansible.builtin.debug:
        msg: "brrewery stub playbook for rtorrent remove"
