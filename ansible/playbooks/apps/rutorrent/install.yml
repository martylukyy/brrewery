---
- hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: rutorrent install placeholder
      ansible.builtin.debug:
        msg: "brrewery stub playbook for rutorrent install"
